import { RmEtiquetasPipe } from './rm-etiquetas.pipe';

describe('RmEtiquetasPipe', () => {
  it('create an instance', () => {
    const pipe = new RmEtiquetasPipe();
    expect(pipe).toBeTruthy();
  });
});
